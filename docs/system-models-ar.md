# مخطط مبسّط لنماذج نظام إدارة اللوجستيات

النظام مملوك لشركة **البوابة للخدمات اللوجستية** فقط. لا يوجد `Tenant` ولا شركات مالكة متعددة؛ هنقرستيشن وكيتا ونينجا وغيرها منصات عملاء تتعامل معها البوابة.

## الصورة العامة

```mermaid
flowchart TB
    COMPANY["البوابة للخدمات اللوجستية<br/>الشركة الوحيدة المالكة للنظام"]
    USERS["مستخدمو النظام<br/>أدوار + صلاحيات + نطاق وصول"]
    EMPLOYEE["الموظف<br/>رقم داخلي + الاسم + الجنسية + تاريخ التعيين"]
    RIDER["ملف الرايدر<br/>الحالة + المدينة المفضلة"]
    OPERATION["التكليف التشغيلي الحالي والتاريخي<br/>المسمى + العمل الفعلي + مدينة الفرع"]
    COMPLIANCE["البيانات النظامية<br/>الكفالة + الإقامة + الرخص"]
    CARDS["بطاقات الرايدر<br/>بطاقة سائق + بطاقة صحية"]
    FINANCIAL["التأمين الطبي وسندات الأمر"]
    DOCS["الوثائق الخاصة<br/>نسخ ملفات محفوظة وغير مكشوفة للعامة"]
    HOUSING["السكن<br/>السعة + المشرفون + تاريخ الإقامة"]
    PLATFORMS["منصات العملاء<br/>هنقرستيشن • كيتا • نينجا • غيرها"]
    CONTRACTS["عقود البوابة مع المنصات"]
    ACCOUNTS["آيديات المنصات<br/>المالك الرسمي للحساب"]
    ASSIGNMENTS["الاستخدام الفعلي للحساب<br/>من استخدمه ومتى"]

    COMPANY --> USERS
    COMPANY --> EMPLOYEE
    COMPANY --> HOUSING
    COMPANY --> CONTRACTS
    EMPLOYEE --> RIDER
    EMPLOYEE --> OPERATION
    EMPLOYEE --> COMPLIANCE
    RIDER --> CARDS
    EMPLOYEE --> FINANCIAL
    EMPLOYEE --> DOCS
    EMPLOYEE -->|"يسكن في"| HOUSING
    PLATFORMS --> CONTRACTS
    CONTRACTS --> ACCOUNTS
    EMPLOYEE -->|"مسجل رسميًا باسمه"| ACCOUNTS
    RIDER -->|"يستخدم فعليًا"| ASSIGNMENTS
    ACCOUNTS --> ASSIGNMENTS
    USERS -->|"يدير حسب الصلاحية"| EMPLOYEE
    USERS -->|"يدير حسب الصلاحية"| CONTRACTS
```

## ملف الموظف والرايدر

```mermaid
flowchart LR
    E["الموظف"] --> S["فترات الكفالة<br/>كفيل حالي واحد + كامل التاريخ"]
    E --> R["تجديدات الإقامة<br/>إقامة حالية واحدة"]
    E --> J["التكليف التشغيلي<br/>المسمى + العمل الفعلي + جدة/الرياض"]
    E --> L["رخص القيادة"]
    L --> LC["نقل خفيف<br/>نسخ وتجديدات"]
    L --> LM["دراجة نارية<br/>نسخ وتجديدات"]
    E --> I["التأمين الطبي<br/>شركة + مستوى + تاريخ التجديد"]
    E --> P["سندات الأمر<br/>يمكن وجود عدة سندات"]
    E --> RP["ملف الرايدر"]
    RP --> RC["بطاقات السائق<br/>سيارة أو دراجة"]
    RP --> HC["البطاقات الصحية<br/>نسخ وتجديدات"]
```

## ملكية آيدي المنصة واستخدامه

```mermaid
sequenceDiagram
    participant Owner as الموظف المسجل باسمه الآيدي
    participant Account as آيدي المنصة
    participant RiderA as الرايدر المستخدم الأول
    participant RiderB as الرايدر المستخدم التالي

    Owner->>Account: تسجيل الملكية الرسمية
    Account->>RiderA: بدء تكليف استخدام فعلي
    RiderA-->>Account: إنهاء التكليف مع حفظ التاريخ
    Account->>RiderB: بدء تكليف جديد
```

الملكية الرسمية لا تتغير لمجرد تبديل المستخدم الفعلي. يمنع النموذج استخدام الحساب نفسه بواسطة رايدرين في الوقت نفسه، كما يمنع وجود أكثر من تكليف منصة نشط للرايدر نفسه.

## قواعد يفهمها غير التقني

- لا يوجد حذف نهائي من التطبيق؛ السجلات التشغيلية تستخدم حذفًا منطقيًا، والتاريخ القديم يبقى محفوظًا.
- يمكن إغلاق الفترة الحالية مرة واحدة، ولا يمكن إعادة فتحها أو تعديل تاريخها القديم.
- الفرع الفعلي هو مدينة التكليف التشغيلي، وليس المدينة المفضلة في ملف الرايدر.
- يمكن للموظف امتلاك رخصة نقل خفيف ورخصة دراجة نارية حاليتين معًا، لكن لا يمكن وجود رخصتين حاليتين من النوع نفسه.
- نقل الكفالة، تجديد الإقامة أو الرخصة أو البطاقة أو التأمين ينشئ سجلًا جديدًا ويحفظ السابق.
- ملفات الإقامة والرخص والبطاقات والسندات والتأمين تُربط بنسخ وثائق، ومسارها مخطط تحت `wwwroot/private` دون كشفه كملفات عامة.
- حقول الأرقام الحساسة مصممة لحفظ النص المشفر، وبصمة بحث دقيقة، وآخر أربعة أرقام للعرض. تنفيذ التشفير نفسه مطلوب قبل إدخال بيانات حقيقية.
- كلمات مرور حسابات المنصات لا تُخزّن كنص واضح؛ تحفظ في نسخ بيانات اعتماد مشفرة قابلة للتدوير.
